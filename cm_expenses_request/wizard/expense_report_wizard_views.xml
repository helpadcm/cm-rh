<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_cm_expense_report_wizard_form" model="ir.ui.view">
        <field name="name">cm.expense.report.wizard.form</field>
        <field name="model">cm.expense.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Generar Reporte de Viáticos">
                <sheet>
                    <group>
                        <group>
                            <field name="date_from"/>
                            <field name="date_to"/>
                        </group>
                        <group>
                            <field name="department_ids" widget="many2many_tags" options="{'no_create': True}"/>
                            <field name="state"/>
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button name="action_print_excel" string="Imprimir Excel" type="object" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_cm_expense_report_wizard" model="ir.actions.act_window">
        <field name="name">Reporte de Viáticos</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">cm.expense.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
